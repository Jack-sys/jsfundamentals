/* ***********
 ? Literals
 *********** */

// JS defines two types of values:
// 1: Fixed values are called Literals
// 2: Variables values are called Variables

// Literals include: arrays, nubers, booleans, objects, & strings

let days = ['monday', 'the weekend'];
console.log(days.toString());

console.log(days.length);

let tired = true;

let soup = {
    a: ' chicken noodle',
    b: 'tomato',
    c: 'beef & barley'
};
console.log(soup.a);


let car = 'honda';

let decimal = 10.2;




